name = "kuyacarlo-portfolio"
compatibility_date = "2026-08-13"

# Static site → Workers Static Assets (Cloudflare's unified platform;
# Pages legacy is folded into Workers as of wrangler 4.x).
[assets]
directory = "./dist"
